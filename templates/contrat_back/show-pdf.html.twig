<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contract</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4; /* Background color */
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff; /* Contract background color */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Box shadow */
        }
        header {
            text-align: center;
            margin-bottom: 20px;
        }
        header img {
            max-width: 200px;
            height: auto;
        }
        h1 {
            text-align: center;
            font-size: 24px;
            margin-top: 20px;
            margin-bottom: 20px;
        }
        .date {
            text-align: right;
            margin-bottom: 20px;
        }
        .date p {
            margin: 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #dddddd;
            padding: 10px;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        td {
            vertical-align: top;
        }
        .contract-info {
            margin-bottom: 30px;
        }
        .signature {
            margin-top: 50px;
            text-align: center;
        }
        .signature p {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <img src="file:///C:/dev/Django_Assurance/public/back/img/logo" alt="Logo">
        </header>

        <h1>Insurance Contract</h1>

        <div class="date">
            <p>Date: {{ "now"|date("Y-m-d") }}</p>
            <p>Where: Elghazzela </p>
        </div>

        <div class="contract-info">
            <p><strong>Contract Details:</strong></p>
            <table>
                <tbody>
                    <tr>
                        <th>Name</th>
                        <td>{{ contrat.nom }}</td>
                    </tr>
                    <tr>
                        <th>First Name</th>
                        <td>{{ contrat.prenom }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ contrat.email }}</td>
                    </tr>
                    <tr>
                        <th>ID</th>
                        <td>{{ contrat.id }}</td>
                    </tr>
                    <tr>
                        <th>Start Date</th>
                        <td>{{ contrat.dateDebutContrat ? contrat.dateDebutContrat|date('Y-m-d') : '' }}</td>
                    </tr>
                    <tr>
                        <th>End Date</th>
                        <td>{{ contrat.datefinContrat ? contrat.datefinContrat|date('Y-m-d') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Address</th>
                        <td>{{ contrat.adresseAssur }}</td>
                    </tr>
                    <tr>
                        <th>Insurance Number</th>
                        <td>{{ contrat.numeroAssur }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>