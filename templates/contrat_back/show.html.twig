<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contrat</title>
</head>

<body>
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ contrat.nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ contrat.prenom }}</td>
            </tr>
            <tr>
                <th>E-mail</th>
                <td>{{ contrat.email }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>DateDebutContrat</th>
                <td>{{ contrat.dateDebutContrat ? contrat.dateDebutContrat|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DatefinContrat</th>
                <td>{{ contrat.datefinContrat ? contrat.datefinContrat|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>AdresseAssur</th>
                <td>{{ contrat.adresseAssur }}</td>
            </tr>
            <tr>
                <th>NumeroAssur</th>
                <td>{{ contrat.numeroAssur }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_contrat_back_index') }}">back to list</a>

    <a href="{{ path('app_contrat_back_edit', {'id': contrat.id}) }}">edit</a>

    {{ include('contrat_back/_delete_form.html.twig') }}
</body>

</html>
