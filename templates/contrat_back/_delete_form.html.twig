<style>
    .btn {
        background-color: #dc3545; /* Bootstrap 'danger' red color for delete buttons */
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 3px;
        cursor: pointer;
        text-decoration: none;
        font-size: 16px;
        transition: background-color 0.3s;
    }
    .btn:hover {
        background-color: #c82333; /* Slightly darker shade on hover */
    }
</style>

<form method="post" action="{{ path('app_contrat_back_delete', {'id': contrat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat.id) }}">
    <button class="btn">Delete</button>
</form>