<form method="post" action="{{ path('app_mecanicien_front_delete', {'id': mecanicien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mecanicien.id) }}">
    <button class="btn">Delete</button>
</form>
