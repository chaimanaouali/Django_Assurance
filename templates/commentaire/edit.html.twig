{% extends 'admin/base-front.html.twig' %}
{% block title %}modifier Commentaire{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <h1 class="mt-5 mb-4">modifier Commentaire</h1>

                {{ include('commentaire/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="mt-3">
                    <a href="{{ path('app_commentaire_index') }}" class="btn btn-primary">Retourner</a>
                </div>

                {{ include('commentaire/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
