<form method="post" action="{{ path('app_reponse_devis_delete', {'idRep': reponse_devi.idRep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse_devi.idRep) }}">
    <button class="btn">Delete</button>
</form>
