<style>
    /* Style for form wrapper */
    .form-wrapper {
        max-width: 2000px;
        margin: 0 auto;
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 5px;
        background-color: #f9f9f9;
    }

    /* Style for form group */
    .form-group {
        margin-bottom: 20px;
    }

    /* Style for form label */
    .form-group label {
        display: block;
        font-weight: bold;
        margin-bottom: 5px;
    }

    /* Style for form input fields */
    .form-group input[type="text"],
    .form-group input[type="date"] {
        width: 100%;
        padding: 8px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 3px;
    }

    /* Style for form button */
    .btn {
        padding: 10px 20px;
        font-size: 16px;
        border: none;
        border-radius: 3px;
        background-color: #007bff;
        color: white;
        cursor: pointer;
    }

    /* Hover effect for button */
    .btn:hover {
        background-color: #0056b3;
    }

    /* Typography */
    h1 {
        font-size: 35px;
        color: #333;
        margin-bottom: 20px;
    }

    /* Links */
    a {
        color: #007bff;
        text-decoration: none;
        margin-top: 20px;
        display: inline-block;
    }

    a:hover {
        text-decoration: underline;
    }
</style>

{{ form_start(form) }}
    <div class="form-wrapper">
        <div class="form-group">
            <label class="form-label" for="{{ form.etat.vars.id }}">Etat:</label>
            {{ form_widget(form.etat, {'attr': {'class': 'form-control'}}) }}
            <div class="error-message">{{ form_errors(form.etat) }}</div>
        </div>

        <div class="form-group">
            <label class="form-label" for="{{ form.decision.vars.id }}">Decision:</label>
            {{ form_widget(form.decision, {'attr': {'class': 'form-control'}}) }}
            <div class="error-message">{{ form_errors(form.decision) }}</div>
        </div>

        <div class="form-group">
            <label class="form-label" for="{{ form.date_reglement.vars.id }}">Date de Règlement:</label>
            {{ form_widget(form.date_reglement, {'attr': {'class': 'form-control'}}) }}
            <div class="error-message">{{ form_errors(form.date_reglement) }}</div>
        </div>

        <div class="form-group">
            <label class="form-label" for="{{ form.delai_reparation.vars.id }}">Delai de Réparation:</label>
            {{ form_widget(form.delai_reparation, {'attr': {'class': 'form-control'}}) }}
            <div class="error-message">{{ form_errors(form.delai_reparation) }}</div>
        </div>

        <div class="form-group">
            <label class="form-label" for="{{ form.duree_validite.vars.id }}">Durée de Validité:</label>
            {{ form_widget(form.duree_validite, {'attr': {'class': 'form-control'}}) }}
            <div class="error-message">{{ form_errors(form.duree_validite) }}</div>
        </div>

        <div class="form-group">
            <label class="form-label" for="{{ form.documents.vars.id }}">Documents:</label>
            {{ form_widget(form.documents, {'attr': {'class': 'form-control'}}) }}
            <div class="error-message">{{ form_errors(form.documents) }}</div>
        </div>

        <div class="form-group">
            <label class="form-label" for="{{ form.email.vars.id }}">Email:</label>
            {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
            <div class="error-message">{{ form_errors(form.email) }}</div>
        </div>

        <button type="submit" class="btn">enregistrer</button>
    </div>
{{ form_end(form) }}
