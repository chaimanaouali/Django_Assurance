<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Traitement Print</title>
    <link rel="stylesheet" href="/style.css" media="all" />
    <style>
        body {
            font-family: 'YourCustomFont', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0; /* Light grey background */
        }

        header {
            text-align: center;
        }

        .clearfix:after {
            content: "";
            display: table;
            clear: both;
        }
        .container img {
            max-width: 40%; /* Reduced the width */
            margin-right: 20px;
            border-radius: 8px;
        }
        .container {
            display: flex;
            align-items: center;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #dcdcdc; /* Light grey background for the container */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            border: 1px solid #000; /* Black border */
        }

     
        .details {
            flex: 1;
            background-color: #dcdcdc; /* Light grey background */
            color: #000; /* Black text color */
            border-radius: 8px;
            padding: 15px;
        }

        h1 {
            color: #007bff;
            font-size: 28px;
            margin-top: 20px;
            margin-bottom: 20px;
            text-align: center;
        }

        hr {
            border: 1px solid #000; /* Black border for the horizontal rule */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            border: 1px solid #000; /* Black border for the table */
        }

        th, td {
            border: 1px solid #000; /* Black border for table cells */
            padding: 15px;
            text-align: left;
            font-size: 16px;
        }

        th {
            background-color: #dcdcdc; /* Light grey background for table headers */
            color: #000;
            font-weight: bold;
        }

        p {
            font-size: 16px;
        }

        strong {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <header class="clearfix">
        <h1>Traitement Details</h1>
        <hr></hr>
   <div class="container">
            
 {% if traitement.photo %}
    <img src="{{ absolute_url(asset('uploads/' ~ traitement.photo)) }}" alt="Traitement Image" />
{% else %}
    <p>No Image</p>
{% endif %}
              
            <div class="details">
                <table>
                    <tr>
                        <th>ID</th>
                        <td>{{ traitement.id }}</td>
                    </tr>
                    <tr>
                        <th>Responsable</th>
                        <td>{{ traitement.responsable }}</td>
                    </tr>
                    <tr>
                        <th>Statut</th>
                        <td>{{ traitement.statut }}</td>
                    </tr>
                    <tr>
                        <th>Date de Traitement</th>
                        <td>{{ traitement.datetaitement ? traitement.datetaitement|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Remarque</th>
                        <td>{{ traitement.remarque }}</td>
                    </tr>
                </table>
            </div>
        </div>
    </header>
</body>
</html>
