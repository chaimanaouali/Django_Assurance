<!DOCTYPE html>
<html>
<head>
    <title>Devis - Assurance Django</title>
    <style>
        /* CSS styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7; /* Couleur de fond */
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #ffffff; /* Couleur de fond de la boîte */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #333333; /* Couleur du titre */
            text-align: center;
            margin-bottom: 30px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #dddddd; /* Couleur de la bordure */
        }
        th {
            background-color: #f0f0f0; /* Couleur de fond de l'en-tête de colonne */
            font-weight: bold;
            color: #333333; /* Couleur du texte de l'en-tête de colonne */
            text-transform: uppercase;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9; /* Couleur de fond des lignes paires */
        }
        tr:hover {
            background-color: #e2e2e2; /* Couleur de fond au survol */
        }
        .company-details {
            margin-bottom: 20px;
        }
        .company-details h2 {
            color: #0066cc; /* Couleur du titre des détails de la société */
        }
        .company-details p {
            color: #666666; /* Couleur du texte des détails de la société */
        }
    </style>
</head>
<body>
<class="clearfix">
    <h1 class="logo me-auto" style="color:green;margin-left:15%">Django</h1>
    <h1 align="center"> Liste des Posts </h1>

    <!-- Company details section -->


    <table>
        <tbody>
            <!-- Contenu du devis ici -->
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>titre</th>
                <td>{{ post.titre }}</td>
            </tr>
            <tr>
                <th>description</th>
                <td>{{ post.description }}</td>
            </tr>
           <tr>
    <th>Image</th>
    <td>
        {% if post.imageName %}
            <img src="{{ absolute_url(asset('uploads/posts/' ~ post.imageName)) }}" alt="Post Image" width="100" height="100">
        {% endif %}
    </td>
</tr>

           
            
            </tr>
        </tbody>
    </table>
    <footer id="footer">

    <div class="footer-top">
      <div class="container">
        <div class="row">

          <div class="col-lg-3 col-md-6 footer-contact">
            <h3>SKILLHUB</h3>
            <p>
              Ghazela,Ariana,Tunisia <br>
              <strong>Phone:</strong> +216 52724070<br>
              <strong>Email:</strong> SKILLHUB@esprit.tn<br>
            </p>
          </div>

          <div class="col-lg-2 col-md-6 footer-links">
            
          </div>
        </div>
</div>
</body>

</html>