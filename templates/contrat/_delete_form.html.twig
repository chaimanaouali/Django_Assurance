<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Delete Contract</title>
<style>
    /* Button styles */
    .btn {
        padding: 8px 15px;
        font-size: 14px;
        background-color: #dc3545; /* Red color for deletion to signify a potentially dangerous action */
        color: white;
        border-radius: 4px;
        border: none;
        cursor: pointer;
        margin-top: 20px; /* spacing */
    }
    .btn:hover {
        background-color: #c82333; /* Darker red on hover */
    }
</style>
</head>
<body>
    <form method="post" action="{{ path('app_contrat_delete', {'id': contrat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat.id) }}">
        <button class="btn">Delete</button>
    </form>
</body>
</html>
